{
  "name": "test-worker",
  "private": true,
  "main": "build/worker/shim.mjs",
  "scripts": {
    "build": "cargo install -q worker-build@^0.7 && worker-build --release"
  },
  "dependencies": {
    "wrangler": "^4.69.0"
  }
}

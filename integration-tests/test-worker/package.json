{
  "name": "test-worker",
  "private": true,
  "main": "build/worker/shim.mjs",
  "scripts": {
    "build": "cargo install -q worker-build@^0.8 && worker-build --release --panic-unwind"
  },
  "dependencies": {
    "wrangler": "^4.69.0"
  }
}
